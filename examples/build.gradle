apply plugin: 'kotlin'
apply plugin: 'org.jetbrains.dokka'

dependencies {
        compile "org.jetbrains.kotlin:kotlin-stdlib-jdk8"
        compile fileTree(dir: new File(rootDir, 'bc jars'), include: '*.jar')
	    compile project(":kcrypto")
        }

